"use strict";
/*//const executeDBQuery = require('./config/database')
/// the things below goes on controller files
async function run() {
    try {
        const res = await database.executeDBQuery(`Select * from teste`);
        console.log(res);
    } catch (err) {
        console.error(err);
    }
}

run();*/ 
