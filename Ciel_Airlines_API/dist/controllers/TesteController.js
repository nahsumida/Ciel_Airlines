"use strict";
/*const database = require('C:/Users/Cliente/Documents/GitHub/Ciel_Airlines/Ciel_Airlines_API/src/config/database.ts')
import oracledb, { Connection, ConnectionAttributes } from 'oracledb';

async function get() {
    try {
        const result = await database.executeDBQuery(`Select * from test`, "get");
        console.log(result);
        return (result.payload);
    } catch (err) {
        console.error(err);
    }
}

module.exports = {get}*/ 
